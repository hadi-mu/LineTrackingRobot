/* LED_init
 * A function to initialise the 2 outputs that are connected to the LEDs
 */
void LED_init(void);

/* LED
 * A function to turn the LEDs ON and OFF*/

void LED1( unsigned char a);
void LED2( unsigned char b);
